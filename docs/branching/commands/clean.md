# Branching Clean Command

Used to clean git repositories.

## Usage

`sc clean`

## Behaviour

- Git: Runs `git clean` in the git repository.
- Repo: Runs `git clean` in all projects and the manifest.