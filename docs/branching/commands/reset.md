# Branching Reset Command

Used to reset git repositories.

## Usage

`sc reset`

## Behaviour

- Git: Runs `git reset` in the git repository.
- Repo: Runs `git reset` in all projects and the manifest.